`.env`

```
DB_NAME=camagru
DB_USER=camagru
DB_PASSWORD=<secret>
DB_ROOT_PASSWORD=<secret>
JWT_SECRET=<secret>

# Mail
APP_URL=http://localhost:8080
MAIL_FROM=no-reply@camagru.local

# SMTP relay used by msmtp (point these at a real relay to actually deliver)
SMTP_HOST=localhost
SMTP_PORT=25
# SMTP_AUTH=on
# SMTP_TLS=on
# SMTP_USER=<user>
# SMTP_PASS=<password>
```